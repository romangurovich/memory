<div class="ui menu">
  <div class="header item">
    Memory
  </div>
  <% if user_signed_in? %>
    <%= link_to 'Dashboard', dashboard_user_path(current_user), class: 'item' %>
  <% end %>
  <a class="item">
    Tests
  </a>
  <%= link_to 'Network', users_path, class: 'item' %>
  <div class="right menu">
    <a class="item">
      FAQ
    </a>
    <% if user_signed_in? %>
      <%= link_to 'My Profile', edit_user_registration_path(current_user), class: 'item' %>
      <%= link_to 'Log Out', destroy_user_session_path, class: 'item', method: :delete %>
    <% end %>
  </div>
</div>